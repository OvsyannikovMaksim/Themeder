<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/navigation_photos">

    <fragment
        android:id="@+id/navigation_photos"
        android:label="@string/title_photos"
        android:name="com.themeder.themeder.ui.PhotosFragment"
        tools:layout="@layout/fragment_photos">
    </fragment>

    <fragment
        android:id="@+id/navigation_settings"
        android:label="@string/title_settings"
        android:name="com.themeder.themeder.ui.SettingsFragment"
        tools:layout="@layout/fragment_settings">
    </fragment>

    <fragment
        android:id="@+id/navigation_gallery"
        android:label="@string/title_gallery"
        android:name="com.themeder.themeder.ui.GalleryFragment"
        tools:layout="@layout/fragment_gallery">
    </fragment>

</navigation>